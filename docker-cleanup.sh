#!/bin/bash# Clean up Docker containers and images for Grow Aloe CRMset -eecho "🧹 Cleaning up Grow Aloe CRM Docker resources..."# Stop and remove containerecho "Stopping container..."docker stop grow-aloe-crm-container 2>/dev/null || echo "Container not running"docker rm grow-aloe-crm-container 2>/dev/null || echo "Container not found"# Remove imageecho "Removing image..."docker rmi grow-aloe-crm 2>/dev/null || echo "Image not found"# Clean up any dangling imagesecho "Cleaning up dangling images..."docker image prune -fecho "✅ Cleanup complete!"echo ""echo "💡 To rebuild and run:"echo "   ./docker-run.sh"